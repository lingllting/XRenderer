#version 330 core
out vec4 finalColor;

uniform vec4 ourColor;
uniform sampler2D texture1;
uniform sampler2D texture2;

in vec3 varyingColor;
in vec2 varyingTexCoord;

void main()
{
	finalColor = mix(texture(texture1, varyingTexCoord), texture(texture2, varyingTexCoord), 0.2);
}